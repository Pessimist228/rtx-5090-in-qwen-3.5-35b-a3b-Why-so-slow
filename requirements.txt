# Харнесс намеренно обходится без CUDA Toolkit.
# Полоса памяти (Этап 2) меряется через PyTorch с его собственным CUDA-рантаймом,
# nvcc для этого не нужен.

# Чтение метаданных GGUF — Этап 4, байты на токен.
gguf>=0.10.0

numpy>=1.24

# PyTorch ставится отдельно, с индексом под нужную CUDA, например:
#   pip install torch --index-url https://download.pytorch.org/whl/cu130
# Версия колеса должна соответствовать драйверу целевой машины.
# torch
