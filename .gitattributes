# Все текстовые файлы хранятся в репозитории с LF, независимо от платформы.
# Иначе на Windows git подставляет CRLF, и файлы уезжают в репозиторий
# с перемешанными переводами строк.
* text=auto eol=lf

*.png binary
*.jpg binary
*.gguf binary
