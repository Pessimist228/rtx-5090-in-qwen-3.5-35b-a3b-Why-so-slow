# harness — замер и разбор single-stream инференса llama.cpp

Реализация `task.md`. Здесь только измерение и диагностика: ни кернелов, ни
правок llama.cpp, ни оптимизации.

> Документация инструмента. Числа в тексте ниже приводились по ходу работы и
> относятся к промежуточным фазам: часть из них снята на другой карте и другом
> драйвере. Итоговые результаты и все актуальные числа в [POST-ru.md](POST-ru.md).

Проверяемая гипотеза: у CUDA-бэкенда llama.cpp есть фиксированная накладная
стоимость порядка 2.5 мс на токен, не масштабирующаяся с полосой памяти.

```
t_измеренное = байты_на_токен / полоса_измеренная + t_остаток
```

## Состояние

| Этап | Что | Статус |
|---|---|---|
| 0 | Окружение и фиксация | готово |
| 1 | Модели, проверка влезаемости в VRAM | готово |
| 2 | Реальная полоса памяти | готово — 250.4 и **1687.0 ГБ/с** |
| 3 | Обвязка над llama-bench | готово — обе фазы сняты |
| 4 | Байты на токен из GGUF | готово — **2.133 ГБ** у MoE вместо 4.32 |
| 5 | Разложение времени, roofline | готово — остаток **1.83 мс**, размах 0.13 |
| 6 | Профилирование, атрибуция остатка | готово — **1553 и 905 ядер/токен**, SM занят 97% |
| 7 | Перплексия | готово — 5.7732 и 5.7338 |
| 8 | Отчёт | готово — `analyze/report.py` |
| — | Весь цикл одной командой | готово — `run_all.sh`, критерий приёмки 1 |
| — | Цена ядра и порог полосы (сверх ТЗ) | готово — порог **16 МиБ**, см. ниже |

## Главный вывод

Фиксированная плата за токен существует, но её природа не та, которую
предполагало ТЗ. Три подозреваемых оттуда отпадают:

- **CUDA-графы захватываются**, в том числе на MoE: 255 запусков на 255 токенов,
  ни одной причины отключения. Критерий приёмки 7 — да. На плотной модели то же
  самое: 127 запусков на 128 токенов.
- **Маршрутизация экспертов ничего не стоит**: у MoE остаток меньше, чем у
  плотной модели, хотя ТЗ ожидало обратного.
- **Дыр планирования нет**: занятость SM в чистом прогоне 97% на 5090 и 96.7%
  на 4060.

Остаток — цена того, что ядра читают слишком мало байт за запуск. Ядро выходит
на полосу памяти не сразу, и порог зависит от карты: на 4060 (24 SM) 90% полосы
начинаются с 16 МиБ, на 5090 (170 SM) — только с 32 МиБ. Дальше всё решает, по
какую сторону этого порога лежит реальное ядро:

| | 4060, плотная 9B | 5090, MoE 35B-A3B |
|---|---|---|
| ядер на токен | 905 | 1553 |
| матвеков на токен | 220 | 434 |
| **байт на один матвек** | **21.8 МБ** | **4.6 МБ** |
| относительно порога 16.8 МБ | выше | **вчетверо ниже** |
| **достигнутая полоса матвеков** | **230 ГБ/с = 92%** | **884 ГБ/с = 52%** |
| цена недобора полосы | 1.67 мс/токен | 1.08 мс/токен |
| прочие ядра | 1.45 мс при 685 запусках | 2.16 мс при 1118 запусках |

Одна и та же сборка llama.cpp на одной карте берёт 92% полосы, на другой — 52%,
и различает их не карта, а размер чтения на ядро. Мелкими ядра на 5090 делает
не гранулярность вообще, а MoE: веса нарезаны на 8 экспертов, и каждый матвек
читает кусок вместо целой матрицы.

Поэтому направление для следующей задачи — не «сливать мелкие ядра подряд», а
**поднимать байты на ядро выше порога**, и на MoE это прежде всего группировка
экспертов в один матвек. Заодно это объясняет, почему графы дали лишь часть
выигрыша и почему MoE ничего не добавил к остатку: узкое место не в запусках.

Одно предупреждение к модели `остаток = C × число_ядер`. Она удобна, но
описывает не механизм: выведенная из неё цена ядра равна 1.18 мкс на 5090 и
3.00 мкс на 4060, то есть между картами не переносится, а слагаемые в таблице
выше частично пересекаются с крышей по памяти. Как оценка порядка она годится,
как основа для расчёта потолка — нет.

## Весь цикл одной командой

```bash
./run_all.sh --model Qwen_Qwen3.5-35B-A3B-Q4_0.gguf --data wiki.test.raw
./run_all.sh --model model.gguf --quick --ppl-chunks 4   # проверка обвязки
```

Проходит Этапы 0, 2–8 подряд и складывает всё в один каталог прогона вместе с
`report.md`. Хост определяется по имени GPU, поэтому команда на ноуте и на
арендованной карте одна и та же — это критерий приёмки 8.

Этап 0 идёт со `--strict`: начинать замеры на машине, где уже что-то не так,
нельзя. Этапы, которых не на чем выполнить, пропускаются с пометкой в итоге, а
не роняют прогон: на ноуте может не оказаться датасета для перплексии, на поде
— nsys. Ошибка `bench.py` прогон прекращает — без матрицы отчёт бессмысленен.

Проверено на арендованной 5090: все восемь этапов отрабатывают подряд без
вмешательства.

## Этап 0 — как устроен

```bash
python -m common.env                      # хост определяется по GPU
python -m common.env --host laptop-4060   # или явно
python -m common.env --strict             # ненулевой код при любой проблеме
```

Печатает сводку и пишет `env.json`. Без этого блока рядом с числами результат
недействителен, поэтому каждый прогон кладёт свой `env.json` в свой каталог.

`--strict` предназначен для `run_all.sh`: прогон не должен начинаться на
машине, где уже что-то не так.

### Что проверяется перед запуском

`validate_env()` ловит то, что делает замеры бессмысленными:

- GPU не соответствует конфигу, либо compute capability != `cuda_arch`
- llama.cpp не загрузил CUDA-бэкенд, либо коммит разошёлся с `pinned_commit`
- GPU уже троттлит или горячий до начала нагрузки
- нет torch (не будет Этапа 2) или нет gguf (не будет Этапа 4)

Троттлинг разбирается по битовой маске `clocks_throttle_reasons.active`.
`GpuIdle` и `DisplayClockSetting` замеру не мешают и в вину не ставятся, в
отличие от `SwPowerCap`, `HwSlowdown` и термических причин. Для мобильной
карты с лимитом 75 Вт это основной источник шума.

## Этап 2 — полоса памяти

```bash
python measure/bandwidth.py --validate
```

Пишет `bandwidth.json` и проставляет `measured_bandwidth_gbs` в `env.json`.

Заголовочное число — **последовательное чтение**: при декоде с батчем 1 каждый
вес читается ровно один раз на матрично-векторный продукт и не
переиспользуется, запись ничтожна. Копирование меряется рядом как проверка
здравого смысла (двигает вдвое больше байт, обгонять чтение не должно).

Результат на RTX 4060 Laptop:

| | ГБ/с | от паспортных 256 |
|---|---|---|
| чтение | **250.4** | 97.8% |
| копирование (чтение+запись) | 226.5 | 88.5% |

97.8% — необычно высоко, поэтому число проверено независимо (`--validate`):

- **развёртка по размеру буфера**: 64 MiB → 245.3, 256 MiB → 248.7,
  1 GiB → 250.3, 1.5 GiB → 250.4. Число не падает с ростом буфера, значит в
  кэш ничего не помещается. Будь наоборот — малый буфер был бы быстрее.
- **согласие ядер**: sum(f32) 250.4, max(f32) 250.4, sum(f16) 250.3.
  Расхождение 0.03% — три разных ядра редукции упираются в одно и то же,
  значит измерена память, а не ядро.

Разброс между выборками 0.1%, просадки к концу теста нет.

### Что это значит для замеров

**Частота памяти держится 8001 МГц под нагрузкой** (проверено до и после).
Поэтому крыша по памяти остаётся верной, даже если карта начнёт троттлить.

**Но бюджет мощности почти выбран уже здесь.** Чистое чтение забирает 67.2 Вт
из 75 при почти простаивающих SM, частота SM при этом 2535 МГц против
паспортных 3105. На реальном инференсе сверху лягут деквантизация и внимание,
карта упрётся в лимит, и частота SM просядет ещё. Память это не затронет, а вот
**остаток раздует** — тепловой троттлинг легко принять за накладные расходы
CUDA-бэкенда, то есть ровно за то, что мы ищем. В Этапе 3 частоту SM надо
писать по каждому прогону и помечать точки, снятые на просевших частотах.

### Проверяемое предсказание

Для Qwen3.5-9B Q4_0 читается ≈4.80 ГБ весов на токен (эмбеддинг исключён):

```
крыша по памяти : 4.80 ГБ / 250.4 ГБ/с = 19.2 мс -> 52.2 т/с
если накладные ~2.5 мс : 21.7 мс -> 46.2 т/с
```

Этап 3 становится проверкой гипотезы, а не просто сбором чисел: получим около
52 т/с — фиксированных накладных нет, около 46 — гипотеза подтвердилась. Число
берётся на нулевой глубине контекста, где KV-кэш ещё не вносит вклада.

**Чем кончилось.** Замерено 43.9 т/с — ниже обеих вилок: накладные не просто
есть, они больше предположенных 2.5 мс (вышло 2.71 мс). Но профиль Этапа 6
показал, что это не плата за запуск: матвеки на 4060 берут 92% полосы, и
недобор этих восьми процентов стоит 1.67 мс из 2.71.

## Цена ядра — порог, за которым укрупнение перестаёт помогать

```bash
python measure/kernel_cost.py                 # всё, ~5 минут
python measure/kernel_cost.py --only size     # только развёртка размера
```

Этап 6 дал число ядер на токен и вывод, что дело в их размере. Но потолок
такого плана задаёт величина, которой в ТЗ нет: минимальный размер чтения, на
котором ядро выходит на полосу. `kernel_cost.py` меряет её напрямую и пишет
`kernel_cost.json`.

Развёртка на 4060 (пик 248.0 ГБ/с, сходится с Этапом 2 независимо — там 250.4):

| размер чтения | 1 МиБ | 4 МиБ | 8 МиБ | 16 МиБ | 32 МиБ | 128 МиБ |
|---|---|---|---|---|---|---|
| ГБ/с | 57 | 162 | 206 | 230 | 239 | 247 |
| от пика | 23% | 65% | 83% | 93% | 96% | 100% |

**Наивная развёртка 64 КиБ…64 МиБ померяла бы кэш, а не память.** L2 равен
32 МиБ у 4060 и 96 МиБ у 5090 — весь диапазон помещается внутрь целиком, плато
«наступило» бы на первых килобайтах. Поэтому каждый запуск читает свой срез
пула в 4 ГиБ, к моменту возврата к срезу тот вытеснен, а верх диапазона поднят
до 256 МиБ. Скрипт проверяет охват холодных данных против L2 и помечает точки,
где запаса не хватило.

Отдельно меряется цена узла CUDA-графа: граф из 50…2000 тривиальных ядер,
наклон T(M). На 4060 вышло **0.95 мкс на узел**. Это пол, а не рабочая
величина: пересечение прямой t(N) с нулём для реального ядра памяти даёт
**5.3 мкс**, и разница между этими числами — розлив по SM и слив хвоста,
которые пустое ядро не платит.

**Цена сериализации не измерена, и её значение из вывода брать нельзя.**
Поточный запуск через Python стоит 9…18 мкс на вызов, а GPU после того, как CPU
отпустил очередь, дорабатывает 0.02 мкс — карта простаивает всё время, и
меряется диспетчеризация Python, а не запуск CUDA. Валиден только графовый
режим: в цикле `replay()` Python нет. Проверка на голодание встроена, такие
точки помечаются сами. Для честного замера нужен C++ или ncu.

## Кривая настоящего ядра — `bench_matvec_cold.cpp`

Синтетика на torch годится, чтобы увидеть форму кривой, но не чтобы назвать
порог: на 4.6 МБ редукция даёт 525 ГБ/с, `torch.mm` — 407, а трасса реального
`mul_mat_vec_q` — 884. Ядро llama.cpp вдвое эффективнее обеих заглушек.

`test-backend-ops` из состава llama.cpp тоже не подходит, хотя гоняет
правильное ядро: в режиме `perf` он **дублирует один и тот же узел** `n_runs`
раз, матрица оседает в L2 и выдаёт 3654 ГБ/с при физических 1687. Любой замер
с переиспользованием тензора меряет кэш, а в декоде каждый вес читается ровно
один раз за токен.

Поэтому здесь своя программа на ggml: N **различных** матриц, рабочий набор
4 ГиБ (42 кэша L2), то же ядро `mul_mat_vec_q`, данные всегда холодные.

```bash
g++ -O2 -o bench_matvec_cold measure/bench_matvec_cold.cpp \
    -I$LLAMA/ggml/include -L$LLAMA/build/bin \
    -lggml -lggml-base -lggml-cuda -Wl,-rpath,$LLAMA/build/bin
./bench_matvec_cold 4096 4.0        # длина строки, рабочий набор в ГиБ
```

Результат на RTX 5090 (q4_0, пик 1677 ГБ/с):

| МБ на ядро | 1.0 | 4.2 | 8.4 | 16.8 | 33.6 | 67.1 | 134 | 537 |
|---|---|---|---|---|---|---|---|---|
| ГБ/с | 350 | 844 | 1106 | 1343 | 1498 | 1587 | 1632 | 1676 |
| % пика | 21% | 50% | 66% | 80% | 89% | 95% | 97% | 100% |

Две независимые сверки сошлись: пик 1677 против измеренных Этапом 2
**1687 ГБ/с** (99%), а точка 4.6 МБ даёт 880 ГБ/с против **884 ГБ/с** из
трассы живой сети — расхождение нулевое. Синтетический замер настоящим ядром
воспроизводит поведение работающей модели.

**Порог задаётся объёмом чтения, а не числом строк.** Контроль: при изменении
длины строки в 16 раз (K = 1024, 4096, 16384) кривая не сдвинулась — на 4.2 МБ
вышло 869, 844 и 836 ГБ/с. Для фузии это существенно: форма слитого ядра
неважна, важны байты, которые оно читает.

### Лестница фузии

Веса 2.007 ГБ на токен, 41 слой. Если оставить K матвеков, каждый читает
2.007 ГБ / K, и полоса берётся из кривой выше:

| матвеков/токен | на слой | МБ/ядро | % пика | матвеки, мс |
|---|---|---|---|---|
| **434** | 10.6 | 4.6 | **53%** | 2.28 — сейчас |
| 205 | 5.0 | 9.8 | 69% | 1.73 |
| 123 | 3.0 | 16.3 | 79% | 1.51 |
| 82 | 2.0 | 24.5 | 85% | 1.41 |
| **57** | **1.4** | 35.2 | **90%** | 1.33 |
| 41 | 1.0 | 49.0 | 92% | 1.30 |

Отсюда цена вопроса: девяносто процентов полосы требуют **менее полутора
матвеков на слой**, то есть сокращения их числа в семь с половиной раз. Ходовое
предположение «семь ядер на блок — жёстко, но реально» дало бы 7 МБ на ядро и
всего около 61% полосы. При этом отдача убывает: переход от 434 к 82 экономит
0.87 мс на токен, а весь остаток пути до одного ядра на слой — ещё 0.11 мс.

## Конфигурация

Между машинами меняется только файл в `config/`, код одинаковый — это критерий
приёмки 8. Хост определяется автоматически по имени GPU
(`gpu.expected_name_substring`), так что команда запуска одна и та же.

| Файл | Машина |
|---|---|
| `config/laptop-4060.json` | Фаза 1: ноут, RTX 4060 8 ГБ, sm_89, плотная Qwen3.5-9B |
| `config/rented-5090.json` | Фаза 2: аренда, RTX 5090 32 ГБ, sm_120, MoE Qwen3.5-35B-A3B |

Матрица прогонов, пороги безопасности и параметры железа лежат в конфиге, а не
в коде.

## Решения по окружению

**llama.cpp не собирается из исходников на Фазе 1.** Взят официальный
release-архив `b9006` (`c5a3bc39b`, CUDA 12.4). Бинарь сам сообщает свой
коммит через `llama-server --version`, так что требование Этапа 0 выполняется
без сборки. ТЗ запрещает править llama.cpp, поэтому сборка из исходников не
дала бы ничего, а стоила бы установки CUDA Toolkit.

`setup/build_llama.sh` написан и параметризован (`--arch 89|120`), он нужен
Фазе 2: под Blackwell (sm_120) требуется CUDA >= 12.8, и готовых бинарей может
не оказаться. Скрипт проверяет версию тулкита до начала сборки.

**CUDA Toolkit не нужен.** Полоса памяти в Этапе 2 меряется через PyTorch с
его собственным CUDA-рантаймом (`torch 2.13.0+cu130`). `nvcc` не требуется.

**Версию llama.cpp опрашивает не `llama-bench`.** Он не понимает `--version`:
печатает usage и выходит с кодом 1. Опрос идёт по `llama-server`, затем
`llama-cli`, затем `llama-perplexity`.

## Известные пробелы

**Nsight Systems есть и на ноуте.** Ставится одной командой вместе с Nsight
Compute, отдельно от CUDA Toolkit — свойство харнесса обходиться без тулкита
сохраняется:

```powershell
winget install --id Nvidia.Nsight.Compute
# nsys: <установка>\host\target-windows-x64\nsys.exe
# ncu:  <установка>\target\windows-desktop-win7-x64\ncu.exe
```

В PATH пакет себя не прописывает, `profile.sh` ожидает `nsys` в PATH — путь
надо добавить руками либо на время прогона.

**ncu на GeForce под Windows требует разрешения.** Из коробки перф-счётчики
доступны только администратору, ncu падает с `ERR_NVGPUCTRPERM`. Лечится
однократно, с правами администратора и **перезагрузкой** — драйвер читает ключ
при инициализации, без ребута изменение не действует:

```powershell
New-ItemProperty -Path "HKLM:\SOFTWARE\NVIDIA Corporation\Global\NVTweak" `
    -Name RmProfilingAdminOnly -PropertyType DWord -Value 0 -Force
```

Тот же эффект даёт панель NVIDIA: Desktop → Enable Developer Settings →
Developer → Manage GPU Performance Counters → allow all users.

**ncu ещё не применялся.** Достигнутая полоса на ядро (92% и 52%) посчитана
делением байт на время из трассы nsys, а не снята счётчиком
`dram__throughput.avg.pct_of_peak_sustained_elapsed`. На 4060 такому счёту
можно верить: профиль не искажает время (22.271 мс/токен под трассировкой
против 22.297 в чистом замере, отношение 1.00). На 5090 отношение 1.43, и
там числа 52% и 75% — вилка, которую закрывает только ncu.

**Порог выхода на полосу снят лишь на 24 SM.** У 5090 их 170, ей нужно больше
байт, чтобы заполниться, и её порог почти наверняка выше 16 МиБ. До замера на
самой 5090 переносить 16 МиБ туда нельзя.

**`setup/fetch_models.sh` (Этап 1) не написан** — модели складывались руками.
До сих пор в «Раскладке» он значился как существующий, это исправлено.

`Qwen3.5-9B-Q4_0.gguf` лежит в `models/`, целостность проверена. `UD-Q4_K_XL`
на Фазе 2 получен, перплексия снята (5.7338), но критерий приёмки 4 численно
не выполнен: опубликовано 194 t/s, измерено 262.68 — расхождение объяснено в
коммите `6c909a7`.

## Что показала разведка GGUF

Посылка `task.md` о том, что Q4_0 даёт «ровно 4.5 бита/вес», а по Q4_K_M
«аналитический расчёт ненадёжен», не подтвердилась. Замеры по реальным файлам
Qwen3.5-9B (427 тензоров в обоих):

| Файл | Состав | Эффективно |
|---|---|---|
| `Q4_0` | Q4_0 76.8%, Q6_K 15.5%, Q5_K 5.2%, Q4_1 2.3%, Q8_0/F32 0.2% | **4.797** бит/вес |
| `Q4_K_M` | Q4_K 58.5%, Q6_K 26.7%, Q5_K 14.6%, Q8_0/F32 0.2% | **5.066** бит/вес |

Смешаны оба. «Ровно 4.5» не бывает ни у одного: 4.5 бит/вес — это свойство
блока Q4_0 (32 веса + fp16-множитель = 18 байт), а не файла целиком.

При этом расчёт одинаково точен для любого кванта: GGUF хранит `ggml_type`
каждого тензора, а у каждого типа фиксированные `block_size`/`type_size`.
Сумма по тензорам сошлась с размером файла с точностью 0.2% (метаданные и
выравнивание) для обоих файлов. Поэтому `bytes_per_token.py` считает байты по
фактическим типам тензоров и не зависит от выбора кванта.

**Эмбеддинги не связаны.** `token_embd.weight` и `output.weight` — разные
тензоры по 1 017 118 720 элементов (vocab 248 320 × 4096). Для байт на токен
это принципиально:

- `output.weight` (Q6_K, 834 МБ) читается целиком каждый токен — включается
- `token_embd.weight` (Q4_0, 572 МБ) — читается одна строка, ~2.3 КБ — исключается

Не исключить эмбеддинг — завысить байты на токен на 10.7%. Для Q4_0 весов
читается не 5.37 ГБ, а ≈4.80 ГБ.

**Кванты для сравнения надо нарезать самому.** У имеющихся файлов разные
рецепты: в `Q4_0` есть 4 тензора `Q4_1`, которых в `Q4_K_M` нет вовсе — значит
файлы собраны разными сборщиками с разной политикой override. Для эксперимента,
где байты на токен служат осью, все варианты должны идти из одного BF16 через
один и тот же `llama-quantize`, иначе в сравнение попадёт разница рецептов.

## Раскладка

```
config/       конфиги машин — единственное, что различается между хостами
common/       config.py (загрузка, поиск моделей, каталоги прогонов)
              env.py     (Этап 0: сбор и валидация окружения)
setup/        build_llama.sh (Фаза 2)
measure/      bandwidth.py, bench.py, profile.sh, perplexity.sh,
              kernel_cost.py (цена ядра и порог выхода на полосу)
analyze/      bytes_per_token.py, decompose.py, attribute.py, report.py
results/      <host>_<gpu>_<model>_<quant>_<timestamp>/
```

## Установка

```bash
pip install -r requirements.txt
# torch ставится отдельно, колесо под драйвер целевой машины:
# pip install torch --index-url https://download.pytorch.org/whl/cu130
```
